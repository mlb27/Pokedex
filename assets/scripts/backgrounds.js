function renderBackgroundColor(type) {
    if (type == "fire") {
        cardRef.classList.add("bg-fire");
        return `bg-fire`;
    }
    if (type == "grass") {
        cardRef.classList.add("bg-grass");
        return `bg-grass`;
    }
    if (type == "water") {
        cardRef.classList.add("bg-water");
        return `bg-water`;
    }
    if (type == "bug") {
        cardRef.classList.add("bg-bug");
        return `bg-bug`;
    }
    if (type == "normal") {
        cardRef.classList.add("bg-normal");
        return `bg-normal`;
    }
    if (type == "ground") {
        cardRef.classList.add("bg-ground");
        return `bg-ground`;
    }
    if (type == "poison") {
        cardRef.classList.add("bg-poison");
        return `bg-poison`;
    }
    if (type == "fairy") {
        cardRef.classList.add("bg-fairy");
        return `bg-fairy`;
    }
    if (type == "electric") {
        cardRef.classList.add("bg-electric");
        return `bg-electric`;
    }
    if (type == "fighting") {
        cardRef.classList.add("bg-fighting");
        return `bg-fighting`;
    }
    if (type == "psychic") {
        cardRef.classList.add("bg-psychic");
        return `bg-psychic`;
    }
    if (type == "flying") {
        cardRef.classList.add("bg-flying");
        return `bg-flying`;
    }
    if (type == "ice") {
        cardRef.classList.add("bg-ice");
        return `bg-ice`;
    }
    if (type == "dragon") {
        cardRef.classList.add("bg-dragon");
        return `bg-dragon`;
    }
    if (type == "dark") {
        cardRef.classList.add("bg-dark");
        return `bg-dark`;
    }
    if (type == "rock") {
        cardRef.classList.add("bg-rock");
        return `bg-rock`;
    }
    if (type == "ghost") {
        cardRef.classList.add("bg-ghost");
        return `bg-ghost`;
    }
}
